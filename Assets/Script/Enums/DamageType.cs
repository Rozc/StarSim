namespace Script.Enums
{
    /// <summary>
    /// 伤害类型，不是属性类型，而是造成伤害的方式
    /// </summary>
    public enum DamageType
    {
        None,
        BasicAttack,
        SkillAttack,
        Ultimate,
        Followup,
        Dot,
    }
}